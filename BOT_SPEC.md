# Telegram бот для продажи и бесплатной выдачи SOCKS5-прокси

Ниже — полное, уточненное описание бота с исправлениями и недостающими деталями. Формулировки ориентированы на реализацию в Python (aiogram + FastAPI) с SQLite и webhook.

**Кратко**
Бот выдаёт один бесплатный SOCKS5-прокси на пользователя и продаёт дополнительные прокси за рубли (оплата через Telegram Stars по внутреннему курсу). Управление прокси — через меню в Telegram. Вся логика хранится в SQLite, а прокси создаются на уже установленном SOCKS5-сервере по API/скриптам.

## 1. Цели и рамки

**Цели**
1. Автоматическая выдача 1 бесплатного SOCKS5-прокси при первом /start.
2. Продажа дополнительных прокси за рубли (оплата через Stars).
3. Управление прокси, паролями, статусами.
4. Минималистичный интерфейс без перегрузки.

**Не входит**
1. Управление самим SOCKS5-сервером (он уже установлен).
2. Клиентский VPN/HTTP/HTTPS — только SOCKS5.
3. Сложные тарифы и подписки (только разовые покупки).

## 2. Пользовательские сценарии

**Сценарий A: новый пользователь**
1. /start
2. Проверка пользователя в БД.
3. Регистрация пользователя.
4. Начисление стартового баланса (на бесплатный период).
5. Создание бесплатного прокси.
6. Выдача ссылки t.me/socks и инструкции.
7. Показ главного меню.

**Сценарий B: существующий пользователь**
1. /start
2. Пользователь уже есть — не выдаём второй бесплатный прокси.
3. Показ главного меню.

**Сценарий C: покупка нового прокси**
1. Пользователь нажимает `Купить новый прокси`.
2. Проверка баланса.
3. Если хватает — списание, создание прокси, выдача ссылки.
4. Если не хватает — предложение пополнить баланс.

**Сценарий D: обновление пароля**
1. Пользователь выбирает прокси.
2. Генерация нового пароля.
3. Обновление на SOCKS5-сервере.
4. Отправка обновлённой ссылки.

**Сценарий E: удаление прокси**
1. Пользователь выбирает прокси.
2. Подтверждение удаления.
3. Деактивация на сервере и пометка в БД.

## 3. Команды и интерфейс

**Команды**
1. `/start` — регистрация/вход, выдача бесплатного прокси при первом запуске.
2. `/help` — инструкция по подключению и FAQ.

**Главное меню** (ReplyKeyboard)
1. 💰 Баланс
2. 🧦 Мои прокси
3. 🖥 Устройства
4. ⭐ Пополнить
5. 🤝 Рефералы
6. ❓ Помощь

**Мои прокси** (Inline buttons)
1. Купить новый прокси
2. Обновить пароль
3. Удалить прокси
4. Показать список

## 4. Выдача прокси и формат ссылки

**Формат**
`https://t.me/socks?server=IP&port=PORT&user=LOGIN&pass=PASSWORD`

**Сообщение при выдаче**
1. Короткое приветствие.
2. Ссылка на прокси.
3. Инструкция: «Нажмите на ссылку → Telegram откроет настройки прокси → Добавьте прокси → Включайте и выключайте в настройках Telegram».
4. Альтернатива ручного ввода (IP, порт, логин, пароль) — на случай, если ссылка не открывается.

## 5. Баланс, валюта и оплаты

**Валюта**
1. Вся валюта в боте — рубли.
2. Баланс хранится в рублях.
3. Оплата происходит в Stars, но конвертируется по курсу из настроек.

**Курс Stars**
1. Курс задаётся в админке.
2. Значение по умолчанию: `1 Star = 1 ₽`.
3. Используется только для пересчёта при оплате.

**Баланс**
1. Показать текущий баланс.
2. Показать количество активных прокси.

**Пополнение**
1. Оплата через Telegram Stars (инвойс).
2. После успешной оплаты — конвертация Stars в рубли по текущему курсу.
3. Любые неуспешные платежи не меняют баланс.
4. В webhook требуется обработка `successful_payment` и валидация суммы.

## 6. Цены и биллинг

**Параметры**
1. Цена создания прокси (разовая) — настраивается в админке.
2. Цена в день за одно прокси — настраивается в админке.
3. Сумма, начисляемая для работы бесплатного прокси — настраивается в админке.

**Правила**
1. При выдаче бесплатного прокси пользователю начисляется стартовый баланс в рублях (например, на N дней работы).
2. Покупка нового прокси списывает разовую цену создания.
3. Ежедневная плата списывается за каждое активное прокси (крон/задача раз в сутки).
4. При нехватке средств прокси переводится в `disabled` до пополнения.

## 7. Ограничения и правила

1. Бесплатный прокси выдаётся только один раз.
2. Нельзя получить второй бесплатный прокси.
3. Покупка возможна только при достаточном балансе.
4. Запросы к proxy-серверу должны быть идемпотентны.

## 8. Хранение и схема БД (SQLite)

**Таблица `users`**
1. `id` INTEGER PRIMARY KEY
2. `tg_id` INTEGER UNIQUE NOT NULL
3. `username` TEXT
4. `ref_code` TEXT (собственный реферальный код)
5. `balance` INTEGER NOT NULL DEFAULT 0 (рубли)
6. `created_at` TEXT NOT NULL
7. `last_seen_at` TEXT
8. `referred_by` TEXT (код, по которому пришёл пользователь)
9. `blocked_at` TEXT
10. `deleted_at` TEXT
9. `deleted_at` TEXT

**Таблица `proxies`**
1. `id` INTEGER PRIMARY KEY
2. `user_id` INTEGER NOT NULL
3. `login` TEXT NOT NULL UNIQUE
4. `password` TEXT NOT NULL
5. `ip` TEXT NOT NULL
6. `port` INTEGER NOT NULL
7. `status` TEXT NOT NULL
8. `is_free` INTEGER NOT NULL DEFAULT 0
9. `device_limit` INTEGER NOT NULL DEFAULT 0
10. `device_count` INTEGER NOT NULL DEFAULT 0
11. `created_at` TEXT NOT NULL
12. `deleted_at` TEXT
13. `last_billed_at` TEXT

**Таблица `payments`**
1. `id` INTEGER PRIMARY KEY
2. `user_id` INTEGER NOT NULL
3. `amount` INTEGER NOT NULL (рубли)
4. `status` TEXT NOT NULL
5. `provider_payment_id` TEXT
6. `payload` TEXT
7. `created_at` TEXT NOT NULL

**Таблица `referral_links`**
1. `id` INTEGER PRIMARY KEY
2. `code` TEXT UNIQUE NOT NULL
3. `name` TEXT
4. `owner_user_id` INTEGER
5. `bonus_inviter` INTEGER NOT NULL DEFAULT 0 (рубли)
6. `bonus_invited` INTEGER NOT NULL DEFAULT 0 (рубли)
7. `limit_total` INTEGER
8. `limit_per_user` INTEGER
9. `created_at` TEXT NOT NULL
10. `disabled_at` TEXT

**Таблица `referral_events`**
1. `id` INTEGER PRIMARY KEY
2. `inviter_user_id` INTEGER NOT NULL
3. `invited_user_id` INTEGER NOT NULL
4. `link_code` TEXT NOT NULL
5. `bonus_inviter` INTEGER NOT NULL (рубли)
6. `bonus_invited` INTEGER NOT NULL (рубли)
7. `created_at` TEXT NOT NULL

**Таблица `settings`**
1. `key` TEXT PRIMARY KEY
2. `value` TEXT NOT NULL

Примеры ключей: `proxy_create_price`, `proxy_day_price`, `free_credit`, `stars_rate`, `ref_bonus_inviter`, `ref_bonus_invited`, `max_active_proxies`, `device_limit`, `referral_enabled`.

**Таблица `processed_updates`** (защита от дублей webhook)
1. `id` INTEGER PRIMARY KEY
2. `update_id` INTEGER UNIQUE NOT NULL
3. `created_at` TEXT NOT NULL

**Индексы**
1. `users.tg_id` UNIQUE
2. `proxies.user_id`
3. `payments.user_id`
4. `referral_events.inviter_user_id`
5. `referral_events.invited_user_id`
6. `referral_links.code` UNIQUE

**Статусы**
1. `proxies.status`: `active`, `disabled`, `deleted`
2. `payments.status`: `pending`, `paid`, `failed`

## 9. Интеграция с SOCKS5-сервером

**Ожидаемая интеграция**
1. Создание пользователя с логином/паролем.
2. Выдача доступного IP/порта.
3. Изменение пароля.
4. Отключение пользователя.

**Требования**
1. Все операции подтверждать ответом сервера.
2. В БД писать только после успешной операции.
3. Любые ошибки — логировать и сообщать пользователю в понятной форме.

## 10. Webhook и архитектура

**Webhook**
1. HTTPS endpoint принимает обновления.
2. Идемпотентность по `update_id`.
3. Ответ Telegram в течение 10 секунд.

**Стек**
1. Python
2. aiogram
3. FastAPI
4. SQLite

**Потоки**
1. Входящий update → обработчик → команды/меню → БД/прокси-сервер.
2. Платежи → валидация → обновление баланса.

## 11. Обработка ошибок и UX

1. Любая ошибка создания прокси = ясное сообщение пользователю и запись в лог.
2. Если прокси-сервер недоступен — показать «Сервис временно недоступен».
3. Повторные нажатия кнопок безопасны и не создают дублей.
4. Все чувствительные данные (логин/пароль) отправлять только в личном чате.

## 12. Безопасность

1. Ограничить доступ к админ-функциям по списку `admin_tg_id`.
2. Логировать действия админа отдельно.
3. Защитить webhook секретным токеном.
4. Не хранить в логах пароли и ссылки полностью.

## 13. Админ-функции и админка

**Доступ**
1. Доступ только для `admin_tg_id` (список в настройках).

**Панель админа (кнопки)**
1. Статистика
2. Пользователи
3. Прокси
4. Платежи
5. Рефералы
6. Настройки
7. Экспорт
8. Рассылка

**Статистика**
1. Всего пользователей / активных за 7 дней.
2. Количество активных прокси.
3. Сумма пополнений за день/неделю/месяц (в рублях).
4. Средний баланс на пользователя.
5. Количество отключённых прокси из-за нехватки баланса.

**Пользователи**
1. Поиск по `tg_id` / username.
2. Просмотр профиля: баланс, кол-во прокси, дата регистрации.
3. Ручная корректировка баланса (плюс/минус).
4. Блокировка пользователя (запрет покупок).
5. Удаление пользователя (мягкое удаление с сохранением истории).

Удаление пользователя означает: выставить `deleted_at`, отключить все его прокси и запретить новые операции.

**Прокси**
1. Список активных/отключённых.
2. Фильтр по пользователю.
3. Принудительное отключение/включение.
4. Смена пароля.

**Платежи**
1. Список платежей, фильтр по статусу.
2. Детали платежа и привязка к пользователю.

**Рефералы**
1. Список ссылок.
2. Кол-во приглашённых по ссылке.
3. Сумма начисленных бонусов.

**Экспорт**
1. CSV пользователей.
2. CSV пользователей с балансами.
3. CSV прокси.
4. CSV платежей.
5. CSV реферальных событий.

**Рассылка**
1. Массовая рассылка всем пользователям.
2. Фильтры: только активные за N дней, только с активными прокси, только с балансом > 0.
3. Предпросмотр и подтверждение перед отправкой.
4. Очередь отправки с ограничением скорости, чтобы не словить лимиты Telegram.

**Настройки (ключевые параметры)**
1. Цена создания прокси (рубли).
2. Цена в день за одно прокси (рубли).
3. Стартовый баланс для бесплатного прокси (рубли).
4. Курс Stars к рублю (по умолчанию 1:1).
5. Бонус приглашающему по умолчанию (рубли).
6. Бонус приглашённому по умолчанию (рубли).
7. Лимит активных прокси на пользователя.
8. Лимит устройств (если используется).
9. Включение/выключение реферальной программы.

Все настройки сохраняются в таблице `settings` и применяются сразу.

## 14. Реферальная программа

**Базовые правила**
1. У каждого пользователя есть реферальная ссылка: `https://t.me/<bot>?start=ref_<code>`.
2. При первом /start, если передан `ref_<code>`, фиксируется `referred_by` и запускается начисление бонусов.
3. Бонусы начисляются только при первом входе (один раз на пользователя).

**Бонусы**
1. Бонус приглашающему (рубли) — настраивается в админке.
2. Бонус приглашённому (рубли) — настраивается в админке.
3. Оба бонуса начисляются при первой регистрации приглашённого.

**Кастомные реферальные ссылки**
1. Админ создаёт ссылку с собственным `code`.
2. Ссылка может быть привязана к конкретному владельцу (owner), которому начисляется бонус приглашающему.
3. Для ссылки задаются лимиты: общий лимит приглашений и лимит приглашений на одного пользователя.
4. Для ссылки задаются индивидуальные бонусы (можно отличать от дефолтных).
5. При превышении лимитов бонусы не начисляются.

**Ограничения**
1. Один пользователь может быть приглашён только один раз.
2. Бонусы не начисляются при повторных попытках /start с разными ссылками.

## 15. Подготовка к расширению

1. Подписки и автосписания.
2. Тарифы и промокоды.
3. Лимит устройств: хранить `device_limit` и `device_count` в `proxies`.

## 16. Исправления и уточнения к исходному описанию

1. Добавлен единый рублёвый баланс и курс Stars по настройке.
2. Введены цены: создание и дневная плата, плюс стартовый баланс для бесплатного прокси.
3. Добавлены таблицы `settings`, `referral_links`, `referral_events`.
4. Добавлены `is_free`, `deleted_at`, `status`, `last_billed_at` в proxies.
5. Добавлены `last_seen_at`, `referred_by` в users и `provider_payment_id` в payments.
6. Указана идемпотентность операций на proxy-сервере.
7. Добавлено поведение на ошибки и недоступность сервиса.

## 17. Что нужно подтвердить отдельно

1. Цена создания прокси и цена в день (рубли).
2. Стартовый баланс для бесплатного прокси (рубли).
3. Курс Stars к рублю (если не 1:1).
4. Лимит устройств (если требуется).
5. Ограничения по количеству активных прокси на пользователя.
6. Реальный API/команды для управления SOCKS5-сервером.
7. Набор админ-команд и формат их выдачи.
